# Adventure asset specification

`adventure-assets.json` is the requirement list. `scripts/generate_assets.py` is the only writer of `assets/` and `assets/manifest.json`.

Do not add runtime filenames by hand. Change the spec, regenerate, then validate.
